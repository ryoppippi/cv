{
  "name": "ryoppippi-cv",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "bun -b run ./scripts/build.ts",
    "update-stars": "bun -b run ./scripts/update-stars.ts",
    "dev": "bun run typst watch ./ryotaro_kimura.typ",
    "compile": "bun run typst compile ./ryotaro_kimura.typ --font-path ./ibm-sans"
  },
  "devDependencies": {
    "@types/bun": "^1.3.6",
    "typst": "^0.10.0-8"
  },
  "dependencies": {
    "p-limit": "^7.2.0"
  }
}
